"""autofigure math — 可选：把声明的公式文本升级为原生 Office Math（薄封装 legacy 工具）。"""

from __future__ import annotations


def main(argv: list[str] | None = None) -> int:
    raise SystemExit("error: math 尚未实现（可选命令，见 legacy/tools/powerpoint_native_math.py）")


if __name__ == "__main__":
    main()
